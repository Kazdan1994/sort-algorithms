function partition(tab) {
    
}

const quickSort = (tab) => {
    let N = tab.length
    let pivot = 0

    if (N > 1) {
        pivot = tab[N - 1]
        partition(tab, )

        return tab
    }

};

const T = [8, 2, 9, 4, 1]

const sortTab = quickSort(T)

console.log(sortTab)
